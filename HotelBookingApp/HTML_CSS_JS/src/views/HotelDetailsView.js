export class HotelDetailsView{constructor(root,{hotel,onBook}){this.root=root;this.hotel=hotel;this.onBook=onBook}
  render(){const section=document.createElement('section');section.className='hotel-details';const back=document.createElement('button');back.className='btn ghost small';back.textContent='← Back';back.addEventListener('click',()=>location.reload());const header=document.createElement('div');header.className='details-header';header.innerHTML=`<h2>${this.hotel.name}</h2><p class="location">${this.hotel.location}</p>`;const gallery=document.createElement('div');gallery.className='details-gallery';gallery.style.backgroundImage=`url(${this.hotel.images[0]||'./assets/placeholder.jpg'})`;const desc=document.createElement('p');desc.className='details-desc';desc.textContent=this.hotel.description;const bookBtn=document.createElement('button');bookBtn.className='btn primary large';bookBtn.textContent=`Book from $${this.hotel.price}`;bookBtn.addEventListener('click',()=>this.onBook(this.hotel));section.appendChild(back);section.appendChild(header);section.appendChild(gallery);section.appendChild(desc);section.appendChild(bookBtn);this.root.appendChild(section);} }
