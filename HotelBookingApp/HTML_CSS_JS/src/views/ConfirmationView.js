export class ConfirmationView{constructor(root,{booking}){this.root=root;this.booking=booking}
  render(){const section=document.createElement('section');section.className='confirmation';section.innerHTML=`\n      <h2>Booking confirmed</h2>\n      <p>Thank you, <strong>${this.booking.guestName}</strong>. Your booking for hotel <strong>${this.booking.hotelId}</strong> is confirmed.</p>\n      <ul>\n        <li>Check-in: ${this.booking.checkIn}</li>\n        <li>Check-out: ${this.booking.checkOut}</li>\n        <li>Rooms: ${this.booking.rooms}</li>\n      </ul>\n      <button class="btn primary" id="done">Done</button>\n    `;section.querySelector('#done').addEventListener('click',()=>location.reload());this.root.appendChild(section)} }
